// Atualizo esse número toda vez que envio uma mudança —
// serve pra você confirmar se o app realmente atualizou no celular.
export const VERSAO = '1.32.1';
export const VERSAO_DESCRICAO = 'Corrige GIF do Bob (angulos inconsistentes) e usa a pose certa (olhando pra cima) no rodape do PDF';
